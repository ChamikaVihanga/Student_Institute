@model IEnumerable<TestWebAppliction.Models.StudentCourseModel>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StudentModel.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BatchModel.BatchNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CourseModel.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentModel.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BatchModel.BatchNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CourseModel.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>

<a href='@Url.Action("Index", "Home")'>
    <input type='button' value='< Back to menu' class="btn btn-dark" />
</a>
